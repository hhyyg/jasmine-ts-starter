import { Message } from '../src/myapp';


describe('myapp3', () => {
    it('hello', () => {
        expect('e').toEqual('message');
    });
    it('hello2', () => {
        expect('e2').toEqual('message');
    });
});